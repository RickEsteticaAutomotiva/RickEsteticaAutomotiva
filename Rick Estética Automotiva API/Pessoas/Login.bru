meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/pessoas/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "usuario@email.com",
    "senha": "senha123"
  }
}

settings {
  encodeUrl: true
}
